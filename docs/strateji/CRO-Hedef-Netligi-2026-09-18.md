# CRO hedef netliği — 2026-09-18

- **Arama yüzeyi:** `/tr/hizmetler/cro` `seo.title`ı tam sorgu biçimini başa aldı ("Dönüşüm oranı optimizasyonu — CRO"); "CRO ajansı" ifadesi artık lede, `seo.description`, `seo.entities` ve karşı-konumlandırma SSS'i olmak üzere dört yüzeyde (önce yalnız SSS'teydi — Keyword-Onceliklendirme U-3). Başlığa GİRMEDİ: `ajansı` ailesi `name` ve `seo.title` yüzeylerinde yasaklı (strateji v1.4 karar 3, `keyword-coverage.test.ts`) — prompt'un önerdiği "CRO ajansı: ..." başlığı bu kuralı ve testi kırıyordu, karar Burak'a açık soru olarak duruyor.
- **SSS sırası:** "CRO ajansı ile dönüşüm optimizasyonu danışmanlığı arasındaki fark nedir?" 12. sıradan 2. sıraya alındı; FAQPage JSON-LD'nin `mainEntity` sırası sayfadan türediği için şema sırası da aynı şekilde değişti.
- **Tazelik sinyali:** `cro.ts` `updatedAt: "2026-09-18"` taşıyor — sitemap `lastmod`ı ve yeni `WebPage.dateModified` alanını (`webPageLd`) aynı kaynaktan besliyor. `Service` düğümüne yazılmadı: `dateModified` schema.org'da bir `CreativeWork` özelliğidir, `Service` değildir.
- **İç link ağırlığı:** `cro-ajansi-nasil-secilir` yazısının girişi ve sonuç bölümü `/hizmetler/cro`'ya "CRO ajansı olarak nasıl çalıştığımız" exact-anchor'ıyla bağlandı (mevcut CRO linkleri korundu). Vaka tarafında GYMWOLVES künyesi `cro` taşımaya devam ediyor; SOYLU AVM künyesine eklenmedi — anlatısında A/B testi ve vazgeçme noktası analizi yok, "ölçüm altyapısı yeniden kuruldu" tek başına CRO işi değil.
- **15 Ekim kontrolü:** GSC 28 günlük pencerede "cro ajansı" sorgusunda `/tr/hizmetler/cro` top-10'a girdi mi, gösterim eski hizmet URL'inden (`/cro-donusum-orani-optimizasyonu/`) ve destek yazısından bu sayfaya kaydı mı. Önkoşul sayfanın indekslenmiş olmasıdır (`Indeks-Denetimi-2026-09-18.md`); indeks gelmemişse ölçüm ertelenir, içerik tarafı tekrar açılmaz.
