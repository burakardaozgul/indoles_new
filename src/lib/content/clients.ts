/**
 * Referans logolarının tek sayısal kaynağı.
 *
 * `client-marquee.tsx` listeyi taşır; vizyon bölümündeki sayaç ve marquee
 * başlığı bu sabitten okur ki iki yerde farklı rakam görünmesin.
 * Liste değiştiğinde burası da güncellenir.
 */
export const CLIENT_LOGO_COUNT = 15;
